const PRESETS = {
  NOT_WHITELISTED: ":x: | You need to be whitelisted to use this command",
  USER_DATA_UNDEFINED: ":x: | Your data is not saved in my system",
  REQUIRED_ARGS_UNDEFINED: ":x: | You have to provide the required arguments",
};

module.exports = {
  PRESETS,
};
